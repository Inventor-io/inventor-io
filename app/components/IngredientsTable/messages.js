/*
 * IngredientsTable Messages
 *
 * This contains all the text for the IngredientsTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IngredientsTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IngredientsTable component!',
  },
});
