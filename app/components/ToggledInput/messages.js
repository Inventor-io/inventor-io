/*
 * ToggledInput Messages
 *
 * This contains all the text for the ToggledInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ToggledInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToggledInput component!',
  },
});
