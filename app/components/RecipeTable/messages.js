/*
 * RecipeTable Messages
 *
 * This contains all the text for the RecipeTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RecipeTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecipeTable component!',
  },
});
