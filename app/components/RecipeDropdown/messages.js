/*
 * RecipeDropdown Messages
 *
 * This contains all the text for the RecipeDropdown component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RecipeDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecipeDropdown component!',
  },
});
