/*
 * AddRecipePage Messages
 *
 * This contains all the text for the AddRecipePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddRecipePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddRecipePage container!',
  },
});
