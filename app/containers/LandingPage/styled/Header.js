import styled from 'styled-components';

const Logo = styled.h1`
  height: 50%;
  position: fixed;
  color: white;
  z-index: 10;
  justify-self: center;
  font-size: 13em;
  position: absolute;
  padding-top: 0.7em;
  font-family: Futuro;
`;

export default Logo;
