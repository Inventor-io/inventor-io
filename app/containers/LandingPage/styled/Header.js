import styled from 'styled-components';

const Logo = styled.h1`
  height: 50%;
  position: fixed;
  text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.4);
  color: #f5f5f5;
  z-index: 10;
  justify-self: center;
  font-size: 13em;
  position: absolute;
  padding-top: 0.7em;
  font-family: Futuro;
`;

export default Logo;
