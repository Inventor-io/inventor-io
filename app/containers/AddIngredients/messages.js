/*
 * AddIngredients Messages
 *
 * This contains all the text for the AddIngredients container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddIngredients';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddIngredients container!',
  },
});
