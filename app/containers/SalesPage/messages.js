/*
 * SalesPage Messages
 *
 * This contains all the text for the SalesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SalesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SalesPage container!',
  },
});
