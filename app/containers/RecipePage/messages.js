/*
 * RecipePage Messages
 *
 * This contains all the text for the RecipePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RecipePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecipePage container!',
  },
});
